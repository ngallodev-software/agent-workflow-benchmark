# Build and verify

Inspect the existing implementation, write a short requirement-to-file plan to `BENCHMARK_PLAN.md`, repair only the contract defects, run the visible tests, perform a focused self-review for the canonical requirements and non-targets, and leave the application runnable for live human assessment. Do not redesign or add dependencies.
