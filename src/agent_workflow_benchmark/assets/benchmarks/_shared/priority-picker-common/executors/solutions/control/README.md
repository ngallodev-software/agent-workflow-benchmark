# Priority Picker

Run tests with `python -m unittest discover -s tests/public -v` and serve with `python -m priority_picker.server --port 8000`.

Priority is computed from impact, urgency, confidence, risk, and effort using the benchmark formula.
