let allItems=[];let current=[];
const list=document.querySelector('#list'),detail=document.querySelector('#detail');
function render(){const q=document.querySelector('#search').value.toLowerCase();const status=document.querySelector('#status').value;const risk=document.querySelector('#risk').value;const sort=document.querySelector('#sort').value;current=allItems.filter(x=>(!q||x.title.toLowerCase().includes(q)||x.description.toLowerCase().includes(q))&&(status==='all'||x.status===status)&&(risk==='all'||String(x.risk)===risk));current.sort((a,b)=>sort==='score'?b.score-a.score:b[sort]-a[sort]);list.innerHTML='';current.forEach((item,i)=>{const button=document.createElement('button');button.className='item';button.dataset.testid='priority-item';button.innerHTML=`<span>${i+1}. ${item.title}</span> — <span class="score">${item.score}</span> (${item.status})`;button.onclick=()=>{detail.innerHTML=`<h2>${item.title}</h2><p>${item.description}</p><p>Impact ${item.impact}; urgency ${item.urgency}; effort ${item.effort}; confidence ${item.confidence}; risk ${item.risk}</p>`};list.append(button)});document.querySelector('#summary').textContent=`${current.length} of ${allItems.length} items`}
fetch('/api/items').then(r=>r.json()).then(data=>{if(data.error)throw new Error(data.error);allItems=data.items;render()}).catch(error=>{list.textContent=`Unable to load backlog: ${error.message}`});
['search','status','risk','sort'].forEach(id=>document.querySelector('#'+id).addEventListener('input',render));
document.querySelector('#export').onclick=()=>{const blob=new Blob([JSON.stringify(current,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='priority-ordering.json';a.click();URL.revokeObjectURL(a.href)};
