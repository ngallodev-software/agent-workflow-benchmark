# Priority Picker starter

This deliberately incomplete standard-library fixture is used by the `priority-picker-v1` comparative benchmark.

Run the public tests with:

```bash
python -m unittest discover -s tests/public -v
```

Run the local server with:

```bash
python -m priority_picker.server --port 8000
```
