# Change Window Planner

Starter repository for the Change Window Planner benchmark.

Implement the Python planning contract in `change_window/planner.py`, replace the placeholder browser UI, add focused public tests, and document the final run/use workflow here.

The supplied `data/change-set.json` is a frozen input and must not be modified.
